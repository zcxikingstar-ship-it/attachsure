{
  "name": "attachsure",
  "version": "1.0.0",
  "private": true,
  "description": "A private, offline attachment-message consistency check before you send.",
  "license": "MIT",
  "scripts": {
    "test": "node --test test/core.test.cjs",
    "check": "node --check core.js && node --check app.js"
  },
  "engines": { "node": ">=20" }
}
